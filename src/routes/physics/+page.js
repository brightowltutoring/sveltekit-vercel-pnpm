export const prerender = false;

// In case I globally turn on SSG (i.e. 'prerender = true' in +layout.js), this page should explicitly not be SSG. The content on this page is user specified via input sliders  — specifically custom html elements containing data parameters with raw latex
